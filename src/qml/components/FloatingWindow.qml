// components/FloatingWindow.qml
import QtQuick
import QtQuick.Controls

Rectangle {
    id: floatingWindow
    // Geometry and visual properties
    
    ColumnLayout {
        // Collection management buttons
        // Input field with autocomplete
        AutoCompleteInput {}
        
        // Action buttons (Sort/Button2)
    }
}